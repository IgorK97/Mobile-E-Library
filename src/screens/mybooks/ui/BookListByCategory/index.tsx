import { View } from "react-native";

interface BookListByCategoryProps {
  CategoryId: number;
}

export const BookListByCategory = ({ CategoryId }: BookListByCategoryProps) => {
  return <View></View>;
};
